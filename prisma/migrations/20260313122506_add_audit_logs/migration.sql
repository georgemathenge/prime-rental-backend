-- CreateEnum
CREATE TYPE "AuditAction" AS ENUM ('USER_LOGIN', 'USER_LOGIN_FAILED', 'USER_CREATED', 'USER_UPDATED', 'USER_ROLE_CHANGED', 'HOUSE_CREATED', 'HOUSE_UPDATED', 'TENANT_CREATED', 'TENANT_UPDATED', 'TENANT_MOVED_OUT', 'INVOICE_CREATED', 'INVOICE_UPDATED', 'INVOICE_CANCELLED', 'BANK_STATEMENT_UPLOADED', 'PAYMENT_MANUALLY_ASSIGNED');

-- CreateTable
CREATE TABLE "AuditLog" (
    "id" TEXT NOT NULL,
    "userId" TEXT NOT NULL,
    "action" "AuditAction" NOT NULL,
    "entity" TEXT NOT NULL,
    "entityId" TEXT,
    "summary" TEXT NOT NULL,
    "meta" JSONB,
    "ip" TEXT,
    "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,

    CONSTRAINT "AuditLog_pkey" PRIMARY KEY ("id")
);

-- AddForeignKey
ALTER TABLE "AuditLog" ADD CONSTRAINT "AuditLog_userId_fkey" FOREIGN KEY ("userId") REFERENCES "User"("id") ON DELETE RESTRICT ON UPDATE CASCADE;
